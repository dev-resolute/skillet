import type { GenerateOptions, SkillResult } from '../types.js';

export async function generateSkill(options: GenerateOptions): Promise<SkillResult> {
  throw new Error('Not implemented yet: ' + options.action);
}
